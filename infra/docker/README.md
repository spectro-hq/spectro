# Docker infrastructure

The production-shaped local stack will eventually contain the static web server, API, ingestion, processor, PostgreSQL, and ClickHouse. It is deferred until the protocol and ingestion boundary are proven.
